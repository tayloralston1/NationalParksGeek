@model IList<Park>
@{
    ViewData["Title"] = "Home Page";
}

<h2>National Parks</h2>


@foreach (var park in Model)
{
<div>
    <div>
    <img src="~/img/parks/@(park.ParkCode).png" /> 
    </div>
    <div>
        <h3>@park.ParkName</h3>
        <p>@park.ParkDescription</p>
    </div>
</div>
}